#!/bin/bash -x

echo -n "Enter the number in ten series :"
read n
echo Your number $n in words :
if [ i -eq 1 && i -le 1000 && i -eq i*10 ]
then

         echo `Unit $n`
        10)echo "Ten $n" ;;
        100)echo "Hundred $n" ;;
        1000)echo "Thousand $n" ;;
	
fi

done
